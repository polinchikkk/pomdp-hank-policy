\section{Дизайн экспериментов}

Для каждого информационного состояния заново подбирается линейное правило ставки. Затем правило фиксируется и оценивается на независимых тестовых траекториях.

Главная метрика:
\[
VOI_{dist}=J(s^{agg})-J(s^{dist}).
\]
