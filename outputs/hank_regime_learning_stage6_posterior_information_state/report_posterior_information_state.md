# Апостериорное информационное состояние

## Что сравнивается

В этом блоке сравниваются пять линейных правил на одних и тех же траекториях.
Меняется только то, какая сводка апостериорной информации передаётся правилу.

- Наблюдаемые переменные.
- Апостериорное среднее скрытого состояния.
- Апостериорное среднее и вероятность стрессового режима.
- Апостериорное среднее, вероятность режима и мера неопределённости.
- Распределительно расширенное состояние.

Правило везде имеет одну и ту же форму:
`i_t = rho_i * i_{t-1} + theta' z_t`.

## Дизайн

- Сценарии: 4.
- Валидационные траектории: 500--509.
- Тестовые траектории: 900--949.
- Сравнение парное: для всех правил используются одни и те же тестовые траектории.

## Главные результаты

- Базовый макроэкономический набор × умеренная разделимость режимов: минимальная средняя потеря у правила «Апостериорное среднее»; апостериорное среднее лучше набора наблюдаемых переменных; добавление вероятности режима дополнительного выигрыша не даёт; добавление меры неопределённости дополнительного выигрыша не даёт; распределительное расширение не даёт устойчивого изменения.
- Базовый макроэкономический набор × высокая разделимость режимов: минимальная средняя потеря у правила «Распределительно расширенное состояние»; апостериорное среднее и набор наблюдаемых переменных почти не различаются; добавление вероятности режима дополнительного выигрыша не даёт; добавление меры неопределённости дополнительного выигрыша не даёт; распределительное расширение улучшает результат.
- Ограниченный информационный набор × умеренная разделимость режимов: минимальная средняя потеря у правила «Распределительно расширенное состояние»; апостериорное среднее лучше набора наблюдаемых переменных; добавление вероятности режима дополнительного выигрыша не даёт; добавление меры неопределённости дополнительного выигрыша не даёт; распределительное расширение улучшает результат.
- Ограниченный информационный набор × высокая разделимость режимов: минимальная средняя потеря у правила «Распределительно расширенное состояние»; набор наблюдаемых переменных лучше апостериорного среднего; добавление вероятности режима дополнительного выигрыша не даёт; добавление меры неопределённости дополнительного выигрыша не даёт; распределительное расширение улучшает результат.

## Качество сигнала режима

Отдельно оценивается сама вероятность стрессового режима. Для этого берутся траектории правила,
которое использует только апостериорное среднее, и по ним считаются квадратическая ошибка, логарифмический балл и доля
правильно распознанных режимов.

- Базовый макроэкономический набор × умеренная разделимость режимов: квадратическая ошибка вероятности режима = 0.1675, логарифмический балл = 0.5030, точность режима = 0.742.
- Базовый макроэкономический набор × высокая разделимость режимов: квадратическая ошибка вероятности режима = 0.0858, логарифмический балл = 0.2831, точность режима = 0.888.
- Ограниченный информационный набор × умеренная разделимость режимов: квадратическая ошибка вероятности режима = 0.2459, логарифмический балл = 0.6856, точность режима = 0.574.
- Ограниченный информационный набор × высокая разделимость режимов: квадратическая ошибка вероятности режима = 0.1710, логарифмический балл = 0.5167, точность режима = 0.752.

## Проверка масштаба коэффициентов

Чтобы убедиться, что нулевые эффекты не вызваны слишком узкой сеткой коэффициентов,
дополнительно повторён тот же линейный эксперимент после стандартизации признаков.
Ниже вынесены ключевые результаты этой короткой проверки.

- Базовый макроэкономический набор × умеренная разделимость режимов, добавление вероятности режима: средняя разность = 0.0000e+00, доля совпадений = 1.00.
- Базовый макроэкономический набор × умеренная разделимость режимов, добавление меры неопределённости: средняя разность = 0.0000e+00, доля совпадений = 1.00.
- Базовый макроэкономический набор × умеренная разделимость режимов, добавление распределительных переменных: средняя разность = 0.0000e+00, доля совпадений = 1.00.
- Базовый макроэкономический набор × высокая разделимость режимов, добавление вероятности режима: средняя разность = 0.0000e+00, доля совпадений = 1.00.
- Базовый макроэкономический набор × высокая разделимость режимов, добавление меры неопределённости: средняя разность = 0.0000e+00, доля совпадений = 1.00.
- Базовый макроэкономический набор × высокая разделимость режимов, добавление распределительных переменных: средняя разность = 0.0000e+00, доля совпадений = 1.00.
- Ограниченный информационный набор × умеренная разделимость режимов, добавление вероятности режима: средняя разность = 0.0000e+00, доля совпадений = 1.00.
- Ограниченный информационный набор × умеренная разделимость режимов, добавление меры неопределённости: средняя разность = 0.0000e+00, доля совпадений = 1.00.
- Ограниченный информационный набор × умеренная разделимость режимов, добавление распределительных переменных: средняя разность = 0.0000e+00, доля совпадений = 1.00.
- Ограниченный информационный набор × высокая разделимость режимов, добавление вероятности режима: средняя разность = 0.0000e+00, доля совпадений = 1.00.
- Ограниченный информационный набор × высокая разделимость режимов, добавление меры неопределённости: средняя разность = 0.0000e+00, доля совпадений = 1.00.
- Ограниченный информационный набор × высокая разделимость режимов, добавление распределительных переменных: средняя разность = 0.0000e+00, доля совпадений = 1.00.

## Проверка через HANK-проекцию

Для четырёх ключевых правил дополнительно проверяется, сохраняются ли знаки сравнений
после пропуска средней траектории ставки через переходный решатель полной HANK-модели
на общем допустимом масштабе.

- Полная проверка для всех четырёх ключевых правил выполнена в 2 из 4 сценариев.
- В остальных сценариях общий слой HANK-проверки не используется, потому что проекция сходится не для всех четырёх правил одновременно.
- В общей HANK-проекции совпадают 5 из 6 проверяемых знаков попарных сравнений.
- Базовый макроэкономический набор × высокая разделимость режимов: ранговая корреляция Спирмена = -0.33, общий масштаб = 1.00.
- Ограниченный информационный набор × высокая разделимость режимов: ранговая корреляция Спирмена = 1.00, общий масштаб = 1.00.

## Интерпретация

Этот блок не утверждает, что условного среднего достаточно всегда.
Он проверяет более узкий вопрос: что именно даёт выигрыш, когда правило получает
не одну точечную оценку, а более богатую конечномерную сводку апостериорной информации.

В текущих расчётах добавление вероятности режима и простой агрегированной меры неопределённости
не улучшает линейное правило сверх апостериорного среднего. Проверка со стандартизацией признаков
не меняет этого вывода. Зато в части сценариев полезным
оказывается распределительное расширение состояния. Поэтому главный вывод здесь такой:
важно не только оценивать скрытое состояние, но и проверять, какая именно конечномерная
сводка этой информации действительно нужна правилу политики.